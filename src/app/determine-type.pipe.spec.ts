import { DetermineTypePipe } from './determine-type.pipe';

describe('DetermineTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DetermineTypePipe();
    expect(pipe).toBeTruthy();
  });
});

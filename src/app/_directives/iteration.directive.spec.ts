import { IterationDirective } from './iteration.directive';

describe('IterationDirective', () => {
  it('should create an instance', () => {
    const directive = new IterationDirective();
    expect(directive).toBeTruthy();
  });
});
